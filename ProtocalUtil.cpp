#include "stdafx.h"
#include "ProtocalUtil.h"


bool CProtocalUtil::SendPackage(const CProtocalPackage& package)
{
	// todo by yejinlong, 构造数据包并发送
	return true;
}

bool CProtocalUtil::ParsePackage(unsigned char* data, int dataLength, CProtocalPackage& package)
{
	// todo by yejinlong, 解析数据包
	return true;
}